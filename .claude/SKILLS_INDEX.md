# Backend skills

Workflow procedures owned by `audit-tools-backend`. Cursor and Claude copies stay in sync by hand.

| Skill                             | Trigger                                                                                                                                              |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mobile-release-policy-test-sync` | Changing COPA/YEE `minimum_supported_version`, `latest_version`, or related force-update fields in `PLAYSPACE_RELEASE_POLICY` / `YEE_RELEASE_POLICY` |

Matching Cursor rule: `.cursor/rules/mobile-release-policy-test-sync.mdc`. Policy facts: `docs/deployment.md` → "Mobile Release Policy Sources".
