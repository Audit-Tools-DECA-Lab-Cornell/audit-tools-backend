"""YEE product Pydantic schemas (request/response contracts).

Mirrors `app/products/playspace/schemas/`. Each module is filled in as the
matching routes are extracted from the top-level routers.
"""
