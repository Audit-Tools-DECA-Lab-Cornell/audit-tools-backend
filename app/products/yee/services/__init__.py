"""YEE product services: business logic separated from the HTTP layer.

Mirrors `app/products/playspace/services/`. Each module is filled in as the
matching routes are extracted from the top-level routers.
"""
